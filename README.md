# Utils

Simple utilities collection.
Each file has one function, and the file name is the same as the function name.

Usage:

Run the app.py file, with the file and arguments you want to use. For example:

```bash
python app.py file_name arg_value etc..
``` 

